import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';
import 'package:wbid/constents/constents.dart';

const photoIcon = Icon(
  Icons.photo,
  size: 60,
  color: myColor3,
);

const photoCameraIcon = Icon(
  Icons.photo_camera,
  size: 60,
  color: myColor3,
);

const subIcon = Icon(
  Icons.minimize,
  size: 30,
);
const addIcon = Icon(
  Icons.add,
  size: 30,
);

const notificationIcon = Icon(
  Icons.add_alert_rounded,
  color: myColor3,
);

const messageIcon = Icon(
  Icons.message,
  color: myColor3,
);

const savedIcon = Icon(
  Icons.favorite,
  color: myColor3,
);

const logInIcon = Icon(
  Icons.login,
  color: myColor3,
);

const logOutIcon = Icon(
  Icons.logout,
  color: myColor3,
);

const helpIcon = Icon(
  Icons.assignment_late_outlined,
  color: myColor3,
);

const contactUsIcon = Icon(
  Icons.phone,
  color: myColor3,
);

const settingIcon = Icon(
  Icons.settings,
  color: myColor3,
);
